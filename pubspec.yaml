name: financeflow
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # Perbarui versi http ke yang lebih baru
  fl_chart: ^0.40.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.7
  cloud_firestore: ^4.17.5
  firebase_storage:
  firebase_analytics:
  firebase_database: ^10.0.9
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  firebase_app_check: 0.2.2
  firebase_auth_platform_interface: ^7.3.0
  firebase_core_platform_interface: ^5.0.0
  intl: ^0.18.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - asset/images/financeflow.png

